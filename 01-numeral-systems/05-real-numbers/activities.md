# Урок 5: Представяне и работа с реални числа - Упражнения

---

## 1. Разгряване: степени на 2 с отрицателен показател

Попълнете таблицата наизуст, без калкулатор:

| $2^{-n}$ | $2^{-1}$ | $2^{-2}$ | $2^{-3}$ | $2^{-4}$ | $2^{-5}$ |
|---|---|---|---|---|---|
| Стойност | ? | ? | ? | ? | ? |

---

## 2. Преобразуване на дробна десетична част в двоична

Преобразувайте "на ръка" (метод на последователно умножение по 2, както в урока) и проверете резултата чрез разгъване по степени на 2.

### Задача А

1. $0.5_{10} = ?_2$
2. $0.75_{10} = ?_2$
3. $0.375_{10} = ?_2$
4. $0.8125_{10} = ?_2$
5. $0.5625_{10} = ?_2$

---

## 3. Смесени числа (цяла + дробна част)

Преобразувайте цялата и дробната част поотделно (цялата – с последователно деление, дробната – с последователно умножение) и съберете резултатите в общ запис.

### Задача Б

1. $6.25_{10} = ?_2$
2. $9.375_{10} = ?_2$
3. $3.8125_{10} = ?_2$

---

## 4. Числа с безкраен двоичен запис

Направете първите 8 стъпки от алгоритъма за умножение по 2 и запишете първите 8 получени цифри. Отбележете изрично, че дробната част не достига `0`, т.е. записът е безкраен.

### Задача В

1. $0.1_{10} = 0.\underline{\hspace{2cm}}\ldots_2$ (първите 8 цифри)
2. $0.3_{10} = 0.\underline{\hspace{2cm}}\ldots_2$ (първите 8 цифри)

---

## 5. Нормализиран запис (плаваща запетая)

Запишете всяко число в нормализирана форма $\pm1.d_1d_2\ldots\times2^e$, като преместите двоичната запетая (по образеца от раздел 3.2 от урока).

### Задача Г

1. $110.01_2 = ?$
2. $1001.011_2 = ?$
3. $11.1101_2 = ?$
4. $0.00101_2 = ?$

---

## 6. Работа с Python

Прогнозирайте какво ще изведе всеки от следните програмни фрагменти, **преди** да ги изпълните, след което проверете сами в Python.

### Задача Д

```python
# 1.
print(0.1 + 0.2)

# 2.
print(0.1 + 0.2 == 0.3)

# 3.
print(1.1 * 3 == 3.3)

# 4.
print(round(0.1 + 0.2, 2) == 0.3)
```

### Задача Е

Напишете функция `is_close(a, b, epsilon=1e-9)`, която връща `True`, ако двете реални числа `a` и `b` са равни в рамките на подадения толеранс, и `False` в противен случай. Използвайте я, за да сравните `0.1 + 0.2` и `0.3`. След това пренапишете същата проверка, като използвате `math.isclose()`.

---

## 7. Смесено предизвикателство

1. Обяснете със свои думи защо изразът `0.1 + 0.2 == 0.3` е `False` в Python, позовавайки се на двоичното представяне на числата (не просто "защото компютрите грешат").
2. Кой от следните толеранси е неподходящо избран за сравнение на числа около единица – `epsilon = 1e-15` или `epsilon = 1e-6`? Обяснете защо толкова малка стойност на epsilon може да е проблематична.
3. Открийте грешката в следното разсъждение и обяснете защо е грешно (или защо всъщност е вярно):

   > "Числото $0.25_{10}$ е дробно десетично число, следователно със сигурност няма точен двоичен запис, точно както $0.1_{10}$."
